import 'package:flutter/material.dart';

class Car extends StatelessWidget {
  const Car({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('card desde archivo separado',style: TextStyle(color: Colors.black),),
    );
  }
}